src/infrastructure/data/InMemoryUserRepository.cpp
строки 8-14: небезопасные учетные данные по умолчанию. 
Создание пользователя "admin" с паролем "admin123" прямо 
в коде является серьезным риском. Такие данные должны 
задаваться через безопасные механизмы конфигурации или 
переменные окружения при первом запуске.

строка 5: прямая зависимость от конкретной реализации 
`PlaintextPasswordHasher`. Репозиторий не должен знать,
какой именно хешер используется. Это нарушает принцип 
инверсии зависимостей.